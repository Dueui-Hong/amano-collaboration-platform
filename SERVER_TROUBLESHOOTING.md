# 서버 접속 안 될 때 해결 방법

## 🚨 현재 상황
서버가 응답하지 않거나 접속이 안 되는 경우

---

## ⚡ 빠른 해결 방법

### 방법 1: 서버 재시작 (권장)

```bash
# 1단계: 기존 프로세스 모두 종료
cd /home/user/webapp
pkill -9 node
pkill -9 -f next

# 2단계: 캐시 삭제
rm -rf .next

# 3단계: 서버 재시작
npm run dev
```

### 방법 2: 특정 포트만 정리

```bash
# 3000 포트 정리
fuser -k 3000/tcp

# 서버 재시작
cd /home/user/webapp
npm run dev
```

### 방법 3: 프로덕션 빌드로 실행

```bash
cd /home/user/webapp

# 빌드
npm run build

# 실행
npm run start
```

---

## 🔍 문제 진단

### 1. 서버가 실행 중인지 확인
```bash
ps aux | grep next
```

**결과 예시:**
- 프로세스가 보이면: 서버 실행 중
- 아무것도 안 보이면: 서버 중지됨

### 2. 포트 사용 확인
```bash
lsof -i :3000
```

**결과 예시:**
- `LISTEN` 보이면: 포트 사용 중
- 아무것도 없으면: 포트 사용 안 됨

### 3. 서버 로그 확인
```bash
# 최근 로그 확인
tail -50 /tmp/nextjs-dev.log

# 또는
tail -50 /tmp/port3000.log
```

---

## 💡 포트 변경하여 실행

3000번 포트가 막혔다면 다른 포트 사용:

```bash
cd /home/user/webapp

# 3001 포트로 실행
PORT=3001 npm run dev

# 또는 package.json 수정
# "dev": "next dev -H 0.0.0.0 --port 3001"
```

**접속:**
- http://localhost:3001
- 또는 GetServiceUrl로 공개 URL 확인

---

## 🛠️ 완전 초기화 (마지막 수단)

모든 방법이 실패하면:

```bash
cd /home/user/webapp

# 1. 모든 프로세스 종료
pkill -9 node
pkill -9 -f next
sleep 5

# 2. 캐시 완전 삭제
rm -rf .next
rm -rf node_modules/.cache
rm -rf .turbo

# 3. 의존성 재설치 (선택사항)
# npm install

# 4. 서버 재시작
npm run dev
```

---

## 📱 접속 URL 확인

서버가 정상 실행되면:

### 로컬 접속
```
http://localhost:3000
```

### 공개 URL 확인 (샌드박스)
```bash
# GetServiceUrl 사용
# 또는 로그에서 확인:
# https://3000-xxxxx.sandbox.novita.ai
```

---

## ⏱️ 서버 시작 시간

- **개발 서버**: 2-5초 (빠름)
- **첫 페이지 로딩**: 10-20초 (느림)
- **프로덕션 빌드**: 2-3분 (매우 느림)

**팁:** 서버 시작 후 20-30초 기다린 후 접속하세요!

---

## 🎯 자동 재시작 스크립트

`start-server.sh` 스크립트가 있다면:

```bash
cd /home/user/webapp
./start-server.sh
```

이 스크립트는:
- 기존 서버 자동 종료
- 포트 정리
- 서버 재시작
- 에러 시 자동 재시도

---

## ❓ 여전히 안 되면

### 1. GitHub에서 최신 코드 받기
```bash
cd /home/user/webapp
git pull origin main
npm install
npm run dev
```

### 2. Cloudflare Pages에 배포
```bash
cd /home/user/webapp
npm run build
npm run deploy
```

### 3. 백업에서 복구
```bash
# 백업 파일이 있다면
cd /home/user
tar -xzf webapp-backup.tar.gz
cd webapp
npm install
npm run dev
```

---

## 📞 긴급 연락처

문제가 계속되면:
1. GitHub 이슈 등록
2. 로그 파일 첨부
3. 에러 메시지 스크린샷

---

**작성일:** 2026-01-27
**업데이트:** 서버 재시작 및 문제 해결 가이드
